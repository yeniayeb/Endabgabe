namespace firework{
  // enums for fixed values
  export enum Speed {
    SLOW = 1,
    MIDDLE = 2,
    FAST = 3
  }

  export enum ScatterSize {
    SMALL = 0.87,
    MEDIUM = 0.92,
    LARGE = 0.96
  }

  export enum Color {
    YELLOW = 60,
    GREEN = 120,
    BLUE = 240,
    RED = 0
  }
}